# Gas Market Forecast Report

Generated on {{ metadata.generated_at }}

## Ensemble Metrics
| Metric | Value |
| --- | --- |
{% for name, value in metrics.items() -%}
| {{ name }} | {{ "%.4f"|format(value) }} |
{% endfor %}

## Risk Gating
- NHC alert: **{{ "ON" if risk.nhc_alert else "OFF" }}** ({{ risk.details["nhc"]["active_storms"] }} active storms, threshold {{ risk.details["nhc"]["threshold"] }})
- WPSR alert: **{{ "ON" if risk.wpsr_alert else "OFF" }}** (latest change {{ "%.2f"|format(risk.details["wpsr"]["latest_change"]) }} vs threshold {{ risk.details["wpsr"]["threshold"] }})

## Risk Flags
- Tightness flag: **{{ "ON" if risk_flags["tightness"]["active"] else "OFF" }}** (draw {{ "%.2f"|format(risk_flags["tightness"]["gasoline_stocks_draw"]) }} mmbbl, refinery util {{ "%.1f"|format(risk_flags["tightness"]["refinery_util_pct"]) }}%)
- NHC flag: **{{ "ON" if risk_flags["nhc"]["active"] else "OFF" }}** (operational alert {{ "ON" if risk_flags["nhc"]["nhc_alert"] else "OFF" }}, analyst override {{ "ON" if risk_flags["nhc"]["analyst_flag"] else "OFF" }})
{% if risk_flags["adjustments"] %}
### Tail Adjustments
{% for note in risk_flags["adjustments"] %}
- {{ note }}
{% endfor %}
{% else %}
No additional tail adjustments applied.
{% endif %}

## Calibration Table
| Forecast Bin | Observed Frequency | Count |
| --- | --- | --- |
{% for row in calibration -%}
| {{ "%.2f"|format(row["forecast_mean"]|default(0, true)) }} | {{ "%.2f"|format(row["outcome_rate"]|default(0, true)) }} | {{ row["count"]|default(0, true) }} |
{% endfor %}

## Posterior Summary
{% for name, value in posterior.items() %}
- {{ name }}: {{ "%.4f"|format(value) }}
{% endfor %}

## Sensitivity Grid
| Threshold | ΔRBOB | ΔAlpha | Prob ≥ Threshold |
| --- | --- | --- | --- |
{% for row in sensitivity -%}
| {{ "%.2f"|format(row["threshold"]) }} | {{ "%.3f"|format(row["rbob_delta"]) }} | {{ "%.3f"|format(row["alpha_delta"]) }} | {{ "%.4f"|format(row["prob_above"]) }} |
{% endfor %}

## Figures
![Forecast vs Actual]({{ figures.forecast }})

![Calibration Curve]({{ figures.calibration }})
